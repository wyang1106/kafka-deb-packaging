# /etc/default/kafka-broker

# Uncomment to enable broker
#START="yes"

# Below settings are set as defaults, edit to suit your needs
# Exports are needed here to survive across multiple sh scripts that are being used
#export KAFKA_LOG4J_OPTS="-Dlog4j.configuration=file:${KAFKA_HOME}/config/log4j.properties"
#export KAFKA_HEAP_OPTS="-Xmx1G -Xms1G"
#KAFKA_CFG_FILE="server.properties"
